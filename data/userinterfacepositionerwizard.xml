<!--suppress XmlUnboundNsPrefix -->
<wizard>
	<step id="start" nextstep="end">
		<text value="NOTE: This is intended for people who cannot disable overscan on their television / display.  Please first try to disable overscan before using this feature.\n\nUSAGE: Adjust the screen size and position settings then press OK to continue." />
		<displaytext value="Configure interface" />
		<config screen="UserInterfacePositioner" module="UserInterfacePositioner" type="ConfigList" />
		<code>
self.clearSelectedKeys()
self.selectKey("OK")
self.selectKey("UP")
self.selectKey("DOWN")
self.selectKey("LEFT")
self.selectKey("RIGHT")
		</code>
		<code pos="after" async="yes">
self.markDone()
self.close()
		</code>
	</step>
</wizard>
